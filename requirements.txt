# Core harness is standard-library only. These are for the demo + charts + tests.
gradio>=4.44,<5
jsonschema>=4.0
matplotlib>=3.7
huggingface_hub>=0.24   # optional: live toxic-bert bias audit
pytest>=7.0             # tests only
